# 지중해-흑해 제국사 시각화 웹페이지 — 기획/설계 문서

작성일: 2026-07-12
작성자: 기획 담당 에이전트
대상 독자: 개발 에이전트 (이 문서 + `data/MAP_DATA_SOURCE.md`를 보고 바로 구현 착수 가능해야 함)

> **범위 안내**: 이 문서는 UI/UX 설계, 타임라인 범위, 지도 데이터 연동, 기술 스택을 다룬다.
> **콘텐츠 데이터 스키마와 실제 콘텐츠(`schema.md`, `content.json`)는 이번 단계에서 보류**한다
> — 사용자가 책 내용을 직접 정리한 `history.md`를 별도로 작성 중이며, 이를 받은 뒤 콘텐츠
> 스키마를 확정할 예정이다. 대신 §3에서 콘텐츠 스키마가 반드시 만족해야 할 **최소 요구사항
> (제약조건)**만 명시해 둔다 — 지도 연동 때문에 반드시 지켜야 하는 부분이라 미리 고정한다.
> 나머지 항목(문단 구조, 핵심 사건 리스트 표현 방식 등)은 `history.md` 도착 후 재설계한다.

---

## 1. 프로젝트 개요

- **목표**: 사용자가 읽은 책 "지중해와 흑해 사이 제국들의 역사"의 내용을 시간축 기반
  인터랙티브 지도로 시각화.
- **핵심 플로우**: 하단 타임라인에서 연도 선택 → 좌측 지도가 그 시점 국경/명칭으로 갱신 →
  지도에서 지역 클릭 → 우측 패널에 해당 지역·시대 콘텐츠 표시.
- **레이아웃**: 좌측(지도) + 우측(콘텐츠 패널) + 하단(타임라인 슬라이더) 3분할, 데스크톱
  우선이나 반응형 고려.

---

## 2. 타임라인 범위 확정

### 2.1 확정 범위

- **시작**: 기원전 3000년 (`startYear = -3000`)
- **종료**: 서기 1920년 (`endYear = 1920`)

### 2.2 근거

- 책의 주제(지중해-흑해 사이 제국들)를 다루기에 자연스러운 시작점은 **청동기 시대 도시국가/
  초기 왕국의 형성기**(수메르 도시국가, 이집트 고왕국 성립 직후, 아나톨리아 초기 청동기)이며,
  `historical-basemaps` 데이터셋에서 이 권역 개체가 유의미하게 나타나기 시작하는 스냅샷은
  `-3000`/`-2000`이다 (그 이전 `-4000`, `-5000` 등은 아직 "Egypt", "Ur" 같은 초기 개체만
  드문드문 존재).
- 종료점은 **오스만 제국의 실질적 해체(1922년 술탄제 폐지, 1923년 튀르키예 공화국 수립)**
  라는 자연스러운 서사적 종착점에 가장 가까운 실제 데이터 스냅샷인 **1920년**으로 정한다.
  (1920년 스냅샷에는 세브르 조약 직후의 오스만 제국 축소 영토, 신생 발칸 국가들, 러시아
  제국 붕괴 이후의 상황이 담겨 있어 서사적으로 자연스러운 종착점이다.)
- 이 범위는 `data/MAP_DATA_SOURCE.md`에서 실측 확인한 53개 스냅샷 연도 중 **41개**를
  포함한다 (목록은 해당 문서 §3-1 참고). 41개 스냅샷은 다음과 같이 밀도가 점점 촘촘해진다:
  - 청동기~고전고대: 300~700년 간격 (`-3000, -2000, -1500, -1000, -700, ...`)
  - 중세: 100~200년 간격 (`..., 1000, 1100, 1200, 1279, 1300, 1400, ...`)
  - 근세 이후: 수십 년 간격 (`..., 1700, 1715, 1783, 1800, 1815, 1880, 1900, 1914, 1920`)
  - 이 비선형 밀도는 실제 역사 변화 속도(근대로 갈수록 국경이 자주 바뀜)와도 자연스럽게
    맞아떨어지므로 별도 보정 없이 그대로 사용한다.

### 2.3 슬라이더 범위와 스냅샷의 관계 (2026-07-26 변경)

> **변경 이력**: 최초 설계는 슬라이더가 `-3000`~`1920` 사이 1년 단위 연속값을 갖고, 지도만
> 가장 가까운 스냅샷으로 "스냅"하는 방식이었다(§5 원안, `data/MAP_DATA_SOURCE.md` §5).
> 그러나 실제 사용해보니 "슬라이더가 가리키는 연도"와 "지도가 실제 보여주는 스냅샷 연도"가
> 달라 혼란스럽다는 피드백(사용자)에 따라, **슬라이더 자체가 41개 스냅샷 연도 위의 인덱스만
> 이동**하도록 단순화했다. 즉 슬라이더 값은 항상 실제 지도 연도와 정확히 일치하며, 중간값은
> 아예 존재하지 않는다. 아래 내용은 이 변경을 반영해 갱신함.

- 슬라이더는 `data/MAP_DATA_SOURCE.md` §3-1의 **41개 스냅샷 연도만**을 값으로 가진다(연속
  정수가 아니라 배열 인덱스 0~40을 이동). 좌우 스텝 버튼/화살표 키는 인덱스 ±1(= 다음/이전
  스냅샷)만큼 이동한다.
- "슬라이더 연도 ≠ 지도 연도"라는 불일치 상태 자체가 존재하지 않으므로, 관련 UI(근사치
  안내 캡션 등)는 제거했다.
- 직접 연도 입력(예: "-2000")은 가장 가까운 스냅샷 연도로 반올림해 이동한다(동률이면 이전
  스냅샷 우선, `nearestSnapshotIndex` 유틸).

---

## 3. 콘텐츠 데이터 스키마 — 이번 단계 요구사항만 (최종 설계는 보류)

사용자가 `history.md`에 책 내용을 직접 정리 중이며, 완료되면 공유받아 콘텐츠 스키마와
`content.json`을 재설계한다. 지금 확정하는 것은 **지도 연동 때문에 반드시 지켜야 하는
제약조건**뿐이다. 개발 에이전트는 아래 제약을 만족하는 한 나머지 필드 구조(문단 개수,
표현 방식 등)를 유연하게 가정하고 뼈대만 구현해두면 된다.

### 3.1 반드시 있어야 하는 필드 (지도 연동 최소 요건)

| 필드 (가칭) | 타입 | 이유 |
|---|---|---|
| `id` | string | 콘텐츠 항목 고유 식별자. 라우팅/상태 참조용. |
| `startYear`, `endYear` | integer (BCE는 음수) | 슬라이더 연도와 비교해 "지금 활성 상태인 콘텐츠"를 판별해야 함 (§2.3, `MAP_DATA_SOURCE.md` §5-5). 연도 0은 사용하지 않는 컨벤션(-1 다음 1)을 데이터셋과 통일. |
| `mapEntityNames` | string[] | **단일 문자열 금지.** 같은 정치체라도 스냅샷 연도마다 GeoJSON의 `NAME` 값이 다르다(예: `Rome`→`Roman Empire`, `Eastern Roman Empire`→`Byzantine Empire`, `MAP_DATA_SOURCE.md` §4 참고). 지도에서 클릭된 폴리곤의 `properties.NAME`과 대소문자 무관 매칭할 수 있도록 이 항목이 해당 정치체가 각 연도에 불릴 수 있는 모든 표기를 배열로 담아야 한다. |
| `approxCenter` | `[lon, lat]` | 이 데이터셋에 독립 폴리곤이 없는 항목(십자군 국가, 폰투스 왕국 등, `MAP_DATA_SOURCE.md` §4-1)을 마커로 대체 표시하기 위한 폴백 좌표. 폴리곤이 있는 항목도 "지도 확대/포커스 이동" 시 중심점으로 재사용 가능하니 전 항목에 채워두는 것을 권장. |

### 3.2 향후 스키마 설계 시 참고할 이미 확인된 제약

- 콘텐츠 연도 범위가 겹치는 경우(예: 로마와 파르티아가 동시대) 우측 패널은 "지도에서 클릭된
  지역"으로 소거하므로 연도만으로 유일하게 정해지지 않는다 — `mapEntityNames` 매칭이 1차
  키, 연도가 2차 필터라는 우선순위를 스키마 설계 시에도 유지할 것.
  개별 콘텐츠 항목은 "언제(연도 범위) + 어디(mapEntityNames/좌표)"의 곱집합으로 유일하게
  식별된다고 가정하고 설계하면 된다.
- `history.md` 발췌문을 나중에 필드 단위로 갈아끼울 수 있도록, 본문 텍스트 필드는 처음부터
  "placeholder 여부"를 표시하는 플래그(예: `source: "placeholder" | "book-excerpt"`)를 두는
  방향을 권장 — 사용자가 이미 이 구조를 염두에 두고 있었으므로 스키마 최종 설계 시 반영.
- 이번 단계에서는 `data/schema.md`, `data/content.json`을 **생성하지 않는다**. 개발
  에이전트는 위 최소 요건만 타입/인터페이스 수준에서 미리 잡아두고(예:
  `src/types/content.ts`에 `MapLinkableContent` 같은 부분 인터페이스), 전체 콘텐츠 스키마와
  실 데이터는 `history.md` 공유 이후 별도 작업으로 이어간다.

---

## 4. 기술 스택 추천

### 4.1 기본 스택

- **빌드 도구**: Vite
- **프레임워크**: React + TypeScript
- **스타일링**: 특별한 디자인 시스템 요구가 없으므로 Tailwind CSS 권장 (지도/패널/슬라이더
  3분할 레이아웃과 반응형 breakpoint 관리에 유틸리티 클래스가 빠름). CSS Modules도 대안.
- **상태 관리**: 전역 상태가 "선택된 연도"와 "선택된 지역 id" 단 2개뿐이므로 Redux 등 무거운
  라이브러리는 불필요. React Context + `useReducer` 또는 Zustand(가벼운 전역 store, 보일러
  플레이트 최소) 중 하나로 충분. **Zustand 권장** — 슬라이더 드래그 중 고빈도 갱신이
  있으므로 selector 기반 구독으로 불필요한 리렌더를 줄이기 쉬움.
- **패키지 매니저**: 프로젝트에 특별한 제약이 없다면 pnpm 권장 (속도, 디스크 효율).

### 4.2 지도 렌더링 라이브러리 — 선정 근거

**Leaflet + `react-leaflet` + GeoJSON 레이어를 1순위로 권장.** 근거:

1. **데이터 특성과의 궁합**: `MAP_DATA_SOURCE.md`에서 확인했듯 실제 사용할 데이터는 연도별
   "이미 완성된 GeoJSON FeatureCollection" 파일이다. 실시간으로 좌표를 계산/투영하는
   복잡한 지리 연산(D3-geo의 강점)이 필요한 게 아니라, **완성된 폴리곤을 스냅샷 단위로
   통째로 교체(swap)하며 렌더링**하는 것이 핵심 요구사항이다. Leaflet의
   `L.geoJSON(data).addTo(map)` 패턴이 정확히 이 용도에 맞고, 레이어 교체(`clearLayers` +
   재추가, 또는 `key` prop으로 컴포넌트 자체를 리마운트)도 단순하다.
2. **클릭 인터랙션**: Leaflet GeoJSON 레이어는 feature별 `onEachFeature`로 클릭 핸들러를
   붙이는 게 표준 패턴이라 "지도에서 지역 클릭 → 우측 패널 갱신" 요구사항과 바로 맞는다.
   D3-geo는 이런 클릭/호버 인터랙션을 직접 SVG 이벤트로 구현해야 해서 상대적으로 손이 더
   간다.
3. **팬/줌**: 지중해-흑해 권역이라는 특정 지리 범위에 사용자가 자유롭게 팬/줌하며 탐색할
   가능성이 높다 — Leaflet은 팬/줌이 기본 내장, D3-geo는 `d3-zoom`을 별도로 붙여야 한다.
4. **성능**: 연도당 폴리곤 수가 수십~수백 개 수준(§실측: world_1600에 639개 feature, 대부분
   호주/북미 원주민 세분화라 우리 관심 권역만 필터링하면 훨씬 적음)이라 Leaflet의 SVG/Canvas
   렌더러로 충분히 부드럽다. `preferCanvas: true` 옵션으로 feature가 많은 연도(1600~1800)의
   렌더링 성능을 미리 확보해두는 것을 권장.
5. **D3-geo를 쓰지 않는 이유**: D3-geo는 "커스텀 투영, 애니메이션 트랜지션(국경이 서서히
   바뀌는 모핑 효과)" 같은 고급 지도 시각화에 강점이 있다. 이번 요구사항(스냅 방식 연도별
   교체, 클릭 인터랙션, 팬줌)에는 과한 스펙이며, 지도 위에 GeoJSON을 얹는 기본 작업에서는
   Leaflet 대비 개발 비용이 더 든다. **다만 향후 "연도가 바뀔 때 국경이 부드럽게 애니메이션
   되는" 연출을 추가하고 싶다면 그때 D3-geo 또는 Leaflet 위에 커스텀 트랜지션 레이어를
   얹는 2단계 고도화로 고려**할 수 있다 (1단계 MVP에서는 불필요).
6. **번들 크기**: Leaflet 코어는 ~40KB(gzip)로 가볍다. `react-leaflet`은 얇은 래퍼라 추가
   비용이 크지 않다.

- **투영법**: `MAP_DATA_SOURCE.md`가 권장하듯 데이터는 WGS84 그대로이며, Leaflet 기본
  투영(Web Mercator, EPSG:3857)으로 별도 변환 없이 바로 사용 가능. (원저작자는 면적보존
  투영을 권장하지만, 이는 정적 인쇄물/분석용 권장이고 인터랙티브 팬줌 웹 지도에서는 Web
  Mercator가 사실상 표준이라 그대로 따라도 무방 — 팬줌 UX와의 호환성이 더 중요.)

### 4.3 기타 라이브러리

- **슬라이더**: 순정 `<input type="range">` + 커스텀 스타일링을 권장 (라이브러리 의존성
  최소화). 드래그 중 고빈도 이벤트 처리가 필요하므로 `onInput`(change가 아니라)을 사용하고
  `requestAnimationFrame` 또는 `useDeferredValue`로 렌더링 부하를 조절. 만약 커스텀 트랙
  마킹(스냅샷 연도 위치에 눈금 표시)이 복잡해지면 `@radix-ui/react-slider`(헤드리스, 접근성
  내장) 도입을 2순위로 고려.
- **애니메이션**: 패널 전환 등 소소한 트랜지션은 CSS transition으로 충분. 필요시
  `framer-motion` 도입 가능하나 MVP 필수는 아님.
- **날짜/연도 포맷팅**: BCE/CE 표기와 "기원전 OOOO년" 한국어 라벨링은 자체 유틸 함수로 충분
  (라이브러리 불필요, 예: `formatYear(-1274) → "기원전 1274년"`).

---

## 5. UI/컴포넌트/인터랙션 설계

### 5.1 레이아웃 와이어프레임 (텍스트)

```
┌──────────────────────────────────────────────────────────────────────┐
│  헤더 바: 책 제목 / 현재 연도 큰 글씨 표시 (예: "기원전 1274년경")        │
├───────────────────────────────────────┬────────────────────────────┤
│                                        │  우측 콘텐츠 패널             │
│                                        │  ┌──────────────────────┐  │
│                                        │  │ 지역/문명 제목          │  │
│                                        │  │ 시대 범위 배지          │  │
│              좌측 지도 영역             │  │ (placeholder/발췌 여부) │  │
│         (Leaflet + GeoJSON 레이어)      │  ├──────────────────────┤  │
│                                        │  │ 본문 (스크롤 가능)      │  │
│         · 폴리곤 클릭 → 하이라이트       │  │ 핵심 사건 타임라인      │  │
│         · 폴리곤 없는 항목 → 핀 마커     │  │ 관련 지역/문명 링크      │  │
│                                        │  └──────────────────────┘  │
│                                        │  (미선택 시: 안내 문구 +     │
│                                        │   현재 연도에 존재하는       │
│                                        │   지역 리스트 표시)          │
├───────────────────────────────────────┴────────────────────────────┤
│  하단 타임라인 슬라이더                                                │
│  [◀] ──●───────────────────────────────────────────────────── [▶]   │
│  기원전 3000년                                              서기 1920년│
│  (스냅샷 연도에 작은 눈금 표시, 현재 스냅샷 연도 라벨 caption)            │
└──────────────────────────────────────────────────────────────────────┘
```

- 좌측 지도: 화면 폭의 약 60~65%, 우측 패널: 약 35~40%. 하단 슬라이더는 전체 폭 고정 높이
  바(약 80~100px, 눈금/라벨 포함).
- 우측 패널은 세로 스크롤 가능한 카드형 레이아웃. 지역 미선택 상태에서는 빈 화면 대신 "현재
  연도에 지도상 존재하는 주요 문명 목록"을 클릭 가능한 리스트로 보여줘 첫 방문자의 탐색을
  유도한다 (콘텐츠 데이터의 `startYear <= 현재연도 <= endYear` 필터 결과).

### 5.2 컴포넌트 구조 (제안)

```
App
├─ Header                      (제목, 현재 연도 큰 표시)
├─ MainLayout
│   ├─ MapPanel
│   │   ├─ HistoricalMap        (Leaflet 인스턴스 wrapper)
│   │   │   └─ GeoJsonLayer     (현재 스냅샷 연도의 FeatureCollection 렌더)
│   │   └─ MapLegend/Caption    ("표시 중: 기원전 1500년 국경 (근사)" 등)
│   └─ ContentPanel
│       ├─ EmptyStateList       (미선택 시: 현재 연도 문명 리스트)
│       └─ ContentDetail        (선택 시: 제목/본문/사건/관련링크)
└─ TimelineSlider
    ├─ SliderTrack               (드래그 가능한 트랙, 스냅샷 눈금)
    ├─ SliderHandle
    ├─ YearInput (선택)          (직접 연도 타이핑 이동, 접근성 보완)
    └─ StepButtons (◀ ▶)        (1년 단위 클릭 이동)
```

### 5.3 상태 관리

전역 상태(Zustand store 예시, 구현 세부는 개발 에이전트 재량):

```ts
interface AppState {
  selectedYear: number;        // -3000 ~ 1920, 슬라이더의 연속값
  snappedYear: number;         // selectedYear를 41개 스냅샷 중 가장 가까운 값으로 스냅한 파생값
  selectedRegionId: string | null; // 지도에서 클릭된 콘텐츠 id (없으면 null → EmptyStateList)
  isDragging: boolean;         // 드래그 중 여부 (지도 리렌더 디바운스 트리거용)
  setYear: (year: number) => void;
  selectRegion: (id: string | null) => void;
}
```

- `snappedYear`는 `selectedYear`의 파생값(useMemo 또는 selector)으로 계산 — 별도 상태로
  이중 관리하지 않는다.
- 지도 GeoJSON 레이어는 `snappedYear`가 실제로 바뀔 때만 리페치/리렌더 (§`MAP_DATA_SOURCE.md`
  §5-3). `selectedYear` 자체의 변화(드래그 중 매 프레임)는 헤더의 연도 라벨과 슬라이더 핸들
  위치만 갱신.
- `selectedRegionId`는 연도가 바뀌어도 유지하되, 만약 새 연도에서 해당 id의 콘텐츠가
  연도 범위를 벗어나면(§3.2) ContentDetail 쪽에서 "당시 기준 설명이 아님" 안내를 보여주거나
  자동으로 `null`로 되돌릴지는 콘텐츠 스키마 확정 후 결정 (현재는 UX 옵션만 남겨둠).

### 5.4 타임라인 슬라이더 인터랙션 상세

- **클릭 이동**: 트랙의 임의 지점을 클릭하면 해당 위치에 대응하는 연도로 즉시 이동(스냅샷
  전환 포함). 좌우 스텝 버튼(◀ ▶)은 정확히 **1년 단위**로 `selectedYear`를 증감.
- **드래그 스크러빙**: 핸들을 눌러서 드래그하는 동안 `selectedYear`는 프레임마다(pointer
  move 이벤트) 연속 갱신하되, 지도 레이어 교체는 `snappedYear` 변경 시에만 발생하므로 자연히
  드래그 중 지도 리렌더 빈도가 제한된다(스로틀 효과가 스냅 로직에서 공짜로 따라옴). 드래그가
  끝나는 시점(`pointerup`)에 최종 `snappedYear` 기준으로 한 번 더 확정 렌더.
- **키보드 접근성**: 슬라이더에 포커스가 있을 때 `←/→` 1년 이동, `Shift+←/→` 10년 이동,
  `Home/End`로 타임라인 시작/끝(-3000/1920)으로 이동. `<input type="range">` 네이티브 동작을
  기반으로 하면 이 대부분이 기본 제공됨.
- **스냅샷 눈금 표시**: 트랙 위에 41개 스냅샷 연도 위치에 작은 tick을 그려, 사용자가 "지도가
  실제로 바뀌는 지점"을 시각적으로 인지할 수 있게 한다 (고대로 갈수록 tick 간격이 넓어지는
  것 자체가 "자료가 성긴 시대"라는 정보를 암묵적으로 전달).
- **직접 연도 입력**: 슬라이더 옆에 작은 숫자 입력(또는 "연도로 이동" 팝오버)을 두어 예:
  "-2000" 또는 "기원전 2000" 타이핑 후 이동 가능하게 하면 먼 과거로 빠르게 점프할 때 드래그
  피로를 줄일 수 있다 (MVP 이후 개선사항으로 남겨도 무방).
- **현재 연도 라벨 포맷**: BCE는 "기원전 N년", CE는 "서기 N년"(또는 단순 "N년") 한국어
  라벨링. 헤더와 슬라이더 캡션에 일관되게 사용.

### 5.5 지도-패널 연동 인터랙션

1. 사용자가 폴리곤 클릭 → `feature.properties.NAME` 획득.
2. 콘텐츠 데이터에서 `mapEntityNames`(대소문자 무관)에 해당 이름이 포함된 항목 검색.
3. 매칭된 항목이 여러 개면(이론상 드묾, 스키마 설계 시 유일성 보장 권장) 첫 번째 항목 사용
   + 콘솔 경고 로그(개발 편의용).
4. 매칭된 항목이 없으면 ContentPanel에 "이 지역에 대한 콘텐츠가 아직 없습니다" 같은
   플레이스홀더 표시 (데이터셋에는 우리 관심 밖의 정치체—예: 오스트레일리아 원주민 집단—도
   다수 포함되어 있으므로 이 케이스는 흔하게 발생함, §`MAP_DATA_SOURCE.md`의 world_1600
   예시 참고).
5. 폴리곤 클릭 외에, 폴리곤이 없는 콘텐츠(십자군 국가 등)를 위한 핀 마커 클릭도 동일하게
   `selectedRegionId`를 갱신.
6. 클릭된 폴리곤은 지도 위에서 강조 스타일(테두리 강조/채우기 색 변경)로 시각적 피드백을
   준다. 재클릭(같은 지역 다시 클릭) 시 선택 해제할지는 UX 취향 — 기본은 유지 권장(같은
   패널을 계속 보며 지도만 팬/줌하는 사용성이 자연스러움).

### 5.6 반응형 / 톤앤매너 권고

- **반응형**: 데스크톱(≥1024px)에서는 좌/우 분할 레이아웃 그대로. 태블릿(768~1023px)에서는
  지도 비중을 살짝 줄이고 패널을 최소 320px 폭 확보. 모바일(<768px)에서는 좌우 분할 대신
  **상하 스택**으로 전환(지도가 위, 패널이 아래, 슬라이더는 그대로 최하단 고정) — 지도
  클릭 시 패널로 자동 스크롤.
- **톤앤매너**: 역사/고문서 느낌을 살리되 과하지 않게 — 배경은 밝은 베이지/양피지 톤이나
  차분한 다크 네이비 중 택1(다크모드 지원 고려 시 둘 다), 폴리곤 색상은 시대/문명군별로
  구분되는 저채도 팔레트(제국별로 색이 매 연도 바뀌면 사용자가 혼란스러우므로, 가능하면
  `mapEntityNames`가 매칭되는 콘텐츠가 있는 폴리곤은 일관된 강조색, 매칭 없는 폴리곤은
  중립 회색조로 처리하는 것을 권장 — 이러면 "콘텐츠가 있는 곳"이 지도에서 시각적으로도
  바로 구분됨).
- **타이포그래피**: 헤더의 큰 연도 표시는 세리프 계열로 "역사서" 느낌을 주고, 본문은
  가독성 좋은 산세리프 권장.
- **성능/로딩 상태**: 스냅샷 전환 시 GeoJSON fetch에 약간의 지연이 있을 수 있으므로(특히
  옵션 A: CDN 런타임 fetch 채택 시) 스켈레톤/스피너보다는 "이전 스냅샷을 유지한 채 살짝
  흐리게(opacity 낮춤) 표시"하는 방식이 자연스러운 전환감을 준다.

---

## 6. 다음 단계 (사용자 history.md 도착 후)

1. `history.md` 내용을 검토해 콘텐츠 스키마(`data/schema.md`)를 §3의 최소 요구사항을
   충족하는 선에서 사용자와 함께 확정.
2. `history.md`의 발췌문을 `content.json`에 매핑하고, 발췌문이 없는 시대/지역은 일반 역사
   지식으로 placeholder 채움 (원래 계획대로, `source` 플래그로 구분).
3. 개발 에이전트가 §4~5 설계를 기반으로 Vite+React+TS 프로젝트 스캐폴딩 및 구현 착수 —
   이 단계는 콘텐츠 스키마 확정을 기다리지 않고 지도/슬라이더/레이아웃 뼈대부터 먼저
   진행 가능 (콘텐츠 연동부만 `MapLinkableContent` 인터페이스로 느슨하게 잡아두고 목업
   데이터로 개발).
